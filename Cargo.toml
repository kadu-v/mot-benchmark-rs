[package]
name = "mot-benchmark-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
jamtrack-rs = { path = "../jamtrack-rs" }
indicatif = "0.17"
image = { version = "0.25.2", default-features = false, features = ["jpeg", "png"] }

[[bin]]
name = "mot_benchmark"
path = "src/mot_benchmark.rs"
